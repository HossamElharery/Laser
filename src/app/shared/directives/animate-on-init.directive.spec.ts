import { AnimateOnInitDirective } from './animate-on-init.directive';

describe('AnimateOnInitDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnInitDirective();
    expect(directive).toBeTruthy();
  });
});
